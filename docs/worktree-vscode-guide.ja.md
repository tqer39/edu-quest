# VS Codeでワークツリーを使った並列開発ガイド

VS Codeのターミナルを使って複数のワークツリーで並列開発する方法を説明します。

## 🎯 概要

VS Codeの内蔵ターミナル機能を使って並列開発が可能です。

## 📋 ステップバイステップガイド

### ステップ1: ワークツリーを作成

VS Codeのターミナル（`Ctrl+\``）を開いて、以下を実行：

```bash
# 3つのワークツリーを作成
just worktree-create refactor-mcp main
just worktree-create refactor-nav main
just worktree-create refactor-dev-version main

# 作成されたワークツリーを確認
just worktree-status
```

### ステップ2: 各ワークツリー用の VS Code ウィンドウを開く

#### オプションA: コマンドパレットから（推奨）

1. `Cmd+Shift+P`（macOS）/ `Ctrl+Shift+P`（Windows）を押す
2. "File: Open Folder" を選択
3. `/Users/takeruooyama/workspace/edu-quest-worktrees/refactor-mcp` を選択
4. 新しいウィンドウで開く

これを各ワークツリーに対して繰り返します。

#### オプションB: コマンドラインから

```bash
# 新しいVS Codeウィンドウで各ワークツリーを開く
code /Users/takeruooyama/workspace/edu-quest-worktrees/refactor-mcp
code /Users/takeruooyama/workspace/edu-quest-worktrees/refactor-nav
code /Users/takeruooyama/workspace/edu-quest-worktrees/refactor-dev-version
```

### ステップ3: 各VS Codeウィンドウで作業

各VS Codeウィンドウで：

1. **ターミナルを開く**: `Ctrl+\``
2. **ターミナルを分割** (オプション): ターミナル右上の分割アイコンをクリック
3. **左ペイン**: 開発サーバーを起動（**ポート自動割り当て**）

   ```bash
   just dev-auto
   ```

   または、従来通りデフォルトポート（8788）で起動：

   ```bash
   pnpm dev:edge
   ```

4. **右ペイン**: Claude Codeを起動

   ```bash
   claude
   ```

**ポート自動割り当ての利点:**

- 複数のワークツリーで同時に開発サーバーを起動できる
- ポート競合を気にする必要がない
- `just dev-auto`コマンドが自動的に空いているポート（8788-8900）を見つけて起動

### ステップ4: ウィンドウ間を切り替えながら作業

- `Cmd+Tab`（macOS）/ `Alt+Tab`（Windows）でVS Codeウィンドウを切り替え
- 各ウィンドウで独立して作業

## 🖥️ レイアウト例

```text
┌──────────────────────────────────────┐
│ VS Code Window 1: refactor-mcp       │
├──────────────────┬───────────────────┤
│ Editor           │ Terminal 1        │
│                  │ $ pnpm dev:edge   │
│                  │ Port: 8788        │
│                  ├───────────────────┤
│                  │ Terminal 2        │
│                  │ $ claude          │
└──────────────────┴───────────────────┘

┌──────────────────────────────────────┐
│ VS Code Window 2: refactor-nav       │
├──────────────────┬───────────────────┤
│ Editor           │ Terminal 1        │
│                  │ $ pnpm dev:edge   │
│                  │ Port: 8789        │
│                  ├───────────────────┤
│                  │ Terminal 2        │
│                  │ $ claude          │
└──────────────────┴───────────────────┘

┌──────────────────────────────────────┐
│ VS Code Window 3: refactor-dev-ver   │
├──────────────────┬───────────────────┤
│ Editor           │ Terminal 1        │
│                  │ $ pnpm dev:edge   │
│                  │ Port: 8790        │
│                  ├───────────────────┤
│                  │ Terminal 2        │
│                  │ $ claude          │
└──────────────────┴───────────────────┘
```

## 🗑️ 作業終了後のクリーンアップ

### 1. 開発サーバーを停止

各VS Codeウィンドウのターミナルで `Ctrl+C`

### 2. VS Codeウィンドウを閉じる

各ウィンドウを閉じる（`Cmd+W` / `Ctrl+W`）

### 3. ワークツリーを削除（必要に応じて）

メインリポジトリのVS Codeに戻って：

```bash
# ワークツリーを削除
just worktree-remove refactor-mcp
just worktree-remove refactor-nav
just worktree-remove refactor-dev-version

# 確認
just worktree-list
```

## 💡 VS Code Tips

### ターミナル操作

| 操作                 | macOS                        | Windows/Linux                |
| -------------------- | ---------------------------- | ---------------------------- |
| **ターミナルを開く** | `Ctrl+\``                    | `Ctrl+\``                    |
| **ターミナルを分割** | ターミナル右上の分割アイコン | ターミナル右上の分割アイコン |
| **ターミナル間移動** | `Cmd+\` + 矢印キー           | `Ctrl+\` + 矢印キー          |
| **新しいターミナル** | `Ctrl+Shift+\``              | `Ctrl+Shift+\``              |

### ワークスペース管理

VS Codeのワークスペース機能を使うと、複数のワークツリーを1つのワークスペースにまとめられます：

1. `Cmd+Shift+P` → "Workspaces: Add Folder to Workspace"
2. 各ワークツリーのフォルダを追加
3. ワークスペースを保存: "File > Save Workspace As..."

## 🔄 よくあるワークフロー

### パターン1: 3つの機能を同時開発

```bash
# 1. ワークツリー作成
just worktree-create feature-a main
just worktree-create feature-b main
just worktree-create feature-c main

# 2. 各ワークツリーをVS Codeで開く
code ../edu-quest-worktrees/feature-a
code ../edu-quest-worktrees/feature-b
code ../edu-quest-worktrees/feature-c

# 3. 各ウィンドウで開発サーバー起動

# 4. 開発

# 5. 完了後、削除
just worktree-remove feature-a
just worktree-remove feature-b
just worktree-remove feature-c
```

### パターン2: 1つの機能に集中 + メインブランチを参照

```bash
# 1. 新機能用のワークツリー作成
just worktree-create new-feature main

# 2. メインリポジトリとワークツリーを両方開く
# メインリポジトリ: 既に開いている
# ワークツリー: 新しいウィンドウで開く
code ../edu-quest-worktrees/new-feature

# 3. ワークツリーで開発、必要に応じてメインブランチを参照

# 4. 完了後
just worktree-remove new-feature
```

## ❓ トラブルシューティング

### Q: ワークツリーが既に存在するエラー

```bash
# ブランチを削除して再作成
git branch -D worktree/feature-name
just worktree-create feature-name main
```

### Q: ポートが既に使用中

```bash
# 使用中のポートを確認
lsof -i :8788

# プロセスをkill
kill <PID>

# または別のポートを指定
just worktree-dev feature-name 8790
```

### Q: Claude Codeが起動しない

```bash
# Claude CLIがインストールされているか確認
which claude

# インストールされていない場合は公式サイトからインストール
```

## 📚 関連ドキュメント

- [完全ガイド (日本語)](./parallel-development.ja.md)
- [クイックリファレンス (日本語)](./worktree-quick-ref.ja.md)
- [Full Guide (English)](./parallel-development.md)
- [Quick Reference (English)](./worktree-quick-ref.md)
